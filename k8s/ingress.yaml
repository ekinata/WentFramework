apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wentframework-ingress
  namespace: wentframework
  annotations:
    kubernetes.io/ingress.class: "nginx"
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "false"
spec:
  rules:
  - host: wentframework.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: wentframework-service
            port:
              number: 80
  # Uncomment for TLS/HTTPS support
  # tls:
  # - hosts:
  #   - wentframework.local
  #   secretName: wentframework-tls
